export const variants=[{name:'rain',slot:'XL_H',fixture:'rain'},{name:'night',slot:'M_H',fixture:'night'}];
export async function prepare(page,context){const fixture=context.variant?.fixture||'clear';await page.addInitScript(({fixture})=>{window.iCUE={defaultTemperatureUnit:()=> '°F',isPreview:true,iCUELanguage:'en'};window.uniqueId='rat-art-lite';window.temperatureUnits='auto';window.theme='sky';window.refreshMinutes=20;window.location1='Phoenix';window.weatherApiKey='';window.__weatherTimelineFixture=fixture;},{fixture});}
export async function ready(page){await page.waitForSelector('#weatherStage[data-ready="true"]');await page.waitForFunction(()=>document.querySelectorAll('.hour').length>0);}
export async function assert(page,context){const data=await page.evaluate(()=>({hours:document.querySelectorAll('.hour').length,setup:!document.getElementById('setupState').hidden,status:document.getElementById('statusLabel').textContent,overflowX:document.documentElement.scrollWidth-innerWidth,overflowY:document.documentElement.scrollHeight-innerHeight}));if(data.hours!==12)throw new Error(`${context.slot}: expected 12 hours, got ${data.hours}`);if(data.setup)throw new Error(`${context.slot}: setup visible`);if(data.overflowX>.5||data.overflowY>.5)throw new Error(`${context.slot}: overflow`);}
